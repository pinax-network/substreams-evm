syntax = "proto3";

package evm.supply.v1;

message Events { repeated Supply supplies = 1; }

message Supply {
  bytes contract = 1; // token contract address
  string total_supply = 2; // totalSupply() -> uint256
  optional string max_supply = 3; // maxSupply() or cap() -> uint256
}
