[package]
name = "dex-pool-state"
description = "Complete-block V2 reserves and ordered V3 pool changes"
edition = { workspace = true }
version = "0.1.0"
license = { workspace = true }

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
substreams = { workspace = true }
prost = { workspace = true }
proto = { path = "../proto" }
